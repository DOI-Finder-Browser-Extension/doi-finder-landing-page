services:
  - type: web
    name: doi-finder-landing
    env: static
    buildCommand: mkdir -p build && cp -r * build/ || true
    staticPublishPath: ./build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
